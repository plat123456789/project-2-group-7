# To run this prject

Run this command:

```
create .env file with following parameters:

DB_NAME=
TEST_DB_NAME=
DB_USERNAME=
DB_PASSWORD=
NODE_ENV=development
PORT=8080


Install NPM Packages

npm install